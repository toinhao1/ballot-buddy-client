import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	mainCard: {
		width: '90%',
		maxHeight: 400,
		margin: 10,
	},
	touchable: {
		borderRadius: 10,
		overflow: 'hidden',
	},
	contentContainer: {
		padding: 10,
	},
});
